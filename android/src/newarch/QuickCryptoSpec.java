package com.quickcrypto;

import com.facebook.react.bridge.ReactApplicationContext;

abstract class QuickCryptoSpec extends NativeQuickCryptoSpec {
  QuickCryptoSpec(ReactApplicationContext context) {
    super(context);
  }
}
